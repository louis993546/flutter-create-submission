import 'state.dart';
import 'action.dart';

AppState appReducer2(AppState state, dynamic action) {
  if (action is ClickAddRecordButton) {
    //TODO do something
    return state;
  } else {
    return state;
  }
}
